{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/main",
            "args": [ "-n", "-5" ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Makefile",
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0"
            },
            "stdio": "src/tail.c"
        }
    ]
}
